name: Fantasy Foosball Weekly Recap

on:
  schedule:
    # Tuesday morning after the fantasy week is complete.
    # 15:00 UTC = 10:00 AM CDT / 9:00 AM CST.
    - cron: '0 15 * * 2'
  workflow_dispatch:
    inputs:
      season:
        description: 'Season to generate (example: 2025)'
        required: false
        default: '2026'
        type: string
      league_id:
        description: 'Sleeper league ID'
        required: false
        default: '1389689189200592896'
        type: string
      week:
        description: 'Specific week. Leave blank to use the latest completed week.'
        required: false
        type: string
      force:
        description: 'Regenerate the recap if this week already exists'
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: fantasy-foosball-weekly-recap
  cancel-in-progress: false

jobs:
  generate-recap:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Generate weekly recap
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: gpt-5.6-luna
          RECAP_SEASON: ${{ inputs.season }}
          RECAP_LEAGUE_ID: ${{ inputs.league_id }}
          RECAP_WEEK: ${{ inputs.week }}
          RECAP_FORCE: ${{ inputs.force }}
        run: node scripts/generate-weekly-recap.mjs

      - name: Commit new recap
        run: |
          git config user.name "fantasy-foosball-bot"
          git config user.email "actions@users.noreply.github.com"
          git add static/recaps
          if git diff --cached --quiet; then
            echo "No new recap to commit."
            exit 0
          fi
          git commit -m "Publish Fantasy Foosball weekly recap"
          git push
